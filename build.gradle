// Top-level build file for RecyScan - German Recycling Assistant
// Project-level Gradle configuration

plugins {
    id 'com.android.application' version '8.2.0' apply false
}

task clean(type: Delete) {
    delete rootProject.buildDir
}
